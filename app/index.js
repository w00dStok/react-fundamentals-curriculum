var React = require('react');
var ReactDOM = require('react-dom');
var HelloWorld = require('./components/Main');
//var routes = require('./config/routes');

ReactDOM.render(
    <HelloWorld />, document.getElementById('app')
);